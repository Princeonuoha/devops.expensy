apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: expensy-ingress
  namespace: student-prince
  annotations:
    cert-manager.io/issuer: letsencrypt
spec:
  ingressClassName: nginx

  tls:
  - hosts:
      - prince.azure.diogohack.shop
    secretName: expensy-tls

  rules:
  - host: prince.azure.diogohack.shop
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 3000